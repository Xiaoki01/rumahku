name: rumahku
description: Aplikasi Monitoring Proyek Konstruksi
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management & Navigation
  get: ^4.7.3
  get_storage: ^2.1.1

  # HTTP & API
  http: ^1.1.0

  # UI Components
  cupertino_icons: ^1.0.6
  intl: ^0.19.0

  # Image Picker
  image_picker: ^1.0.7
  permission_handler: ^11.0.1
  
  # Optional: Permission handler
  # permission_handler: ^11.2.0

  # Optional:
  # dio: ^5.4.0
  # http_parser: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  